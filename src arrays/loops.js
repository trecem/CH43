/* --- Ciclo for. Permite iterar sobre un bloque de código, evaluando condiciones e incrementando contador. 
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}
*/

for (let cuenta = 1; cuenta <= 5; cuenta++) {
    console.log(cuenta);
    
}